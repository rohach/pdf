import React from "react";
import "../styles/Header.css";

const Header = () => {
  return (
    <header className="header">
      <h1>📑 Simple PDF Tool</h1>
      <p>Merge, Split, and Compress your PDFs with ease</p>
    </header>
  );
};

export default Header;
